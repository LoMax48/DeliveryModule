{% extends 'base.html.twig' %}

{% block title %}Регистрация{% endblock %}

{% block body %}
    <h1>Регистрация в модуле</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.crmUrl) }}
        {{ form_row(registrationForm.apiKey) }}
        {{ form_row(registrationForm.deliveryLogin) }}
        {{ form_row(registrationForm.deliveryPassword) }}
        {{ form_row(registrationForm.deliveryIKN) }}
        <button type="submit" class="btn btn-outline-primary">Register</button>
    {{ form_end(registrationForm) }}
{% endblock %}
