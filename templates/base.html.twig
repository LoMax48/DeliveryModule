<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}PickPoint module{% endblock %}</title>
        <link rel="icon" href="{{ asset('images/retailcrm.ico') }}">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <div class="navbar-brand">
                    <img src="{{ asset('images/retailcrm.png') }}" alt="" height="30px">
                    <img src="{{ asset('images/pickpoint.png') }}" alt="" height="30px">
                    Модуль службы доставки PickPoint
                </div>
            </div>
        </nav>
        {% block body %}{% endblock %}
    </body>
</html>
